# KLビリヤード部 ホームページ（システム構成）

本システムは、Dockerコンテナを利用した「フロントエンド（Nginx）」と「バックエンド（Python / FastAPI）」の2コンテナ構成で構築されています。
外部からのアクセスはリバースプロキシおよびCloudflare Tunnelを経由して安全に処理されます。

##  システム構造図 (Architecture)

外部からのリクエストがどのように処理され、各コンテナへ流れるかの全体像です。

インターネット (ユーザー)
│
▼ [HTTPS / 443]
Cloudflare Tunnel
│
▼ (内部ネットワーク転送)
Nginx Proxy Manager (総合受付 / webpanel)
│
▼ [HTTP / 8501]
┌────────────────────────────────────────────────────────┐
│ Ubuntu Server (Dockerホスト)                            │
│                                                        │
│  ┌────────────────────────┐    ┌────────────────────┐  │
│  │ billiards-frontend     │    │ billiards-backend  │  │
│  │ (Nginx)                │    │ (Python / FastAPI) │  │
│  │                        │    │                    │  │
│  │ 　  画面データ配付       │    │ 　  メール送信処理  │  │
│  │    (HTML / CSS / JS)   │    │    (Gmail API)     │  │
│  │                        │    │                    │  │
│  │  　/api への通信を  ────┼───►│ ポート: 8000       │  │
│  │    バックエンドへ転送    │    │                    │  │
│  └────────────────────────┘    └────────────────────┘  │
└────────────────────────────────────────────────────────┘

## 各コンテナの役割と内部詳細

### 1. フロントエンド基盤 (`billiards-frontend`)
* **ベースイメージ:** `nginx:alpine` (またはカスタムビルドイメージ)
* **役割:** * ユーザーへホームページの画面データ（HTML, CSS, JavaScript）を高速に配付。
  * **リバースプロキシ機能:** ユーザーからのアクセスをURLごとに賢く振り分ける交通整理役。
* **ポート転送:** ホストの `8501番` ➡ コンテナ内部の `80番(HTTP)`
* **内部ルーティング仕様:**
  * `/` (ルートアクセス): サーバー内の静的ファイル（`index.html`等）を返却。
  * `/api` (APIリクエスト): 同一ネットワーク内のバックエンドコンテナ (`http://billiards-backend:8000`) へ通信を自動転送。
* **特殊キャッシュ対策:** ユーザーのブラウザに古い画面が残るのを防ぐため、Nginx側で「`.html` ファイルのみキャッシュを完全に禁止（`Cache-Control: no-store`）」する設定を組み込んでいます。

### 2. バックエンド基盤 (`billiards-backend`)
* **ベースイメージ:** `python:3.11-slim` などのPython環境
* **主要ライブラリ:** `FastAPI`, `Uvicorn`
* **役割:** * お問い合わせフォームから送られてきたデータの処理。
  * 送信された内容をバリデーション（チェック）し、Googleのセキュアな認証（アプリパスワード等）を経由して、指定のGmailアドレス宛にお問い合わせ通知メールを自動送信。
* **ポート転送:** ホストの `8000番` ➡ コンテナ内部の `8000番`

---

## 技術的な特徴（アピールポイント）

1. **ドメインの一本化 (Same-Origin 構成):**
   フロントエンドのNginxが `/api` の通信を裏でPythonに転送しているため、ブラウザから見ると画面の取得もメールの送信もすべて `billiards.cyber98t.com` という1つのドメインだけで完結しています。これにより、Web開発で厄介な **CORSエラー（クロスドメイン制限）を根本から回避** しています。
2. **キャッシュバスター (`?v=X`) の導入:**
   JavaScriptをアップデートした際、ユーザー側で手動のキャッシュクリアを必要とせず、自動的に最新のプログラムが強制適用される仕組みを構築しています。
3. **コンテナ間ネットワーク:**
   Dockerの内部ネットワーク機能により、フロントエンドからバックエンドへの転送は `localhost` ではなく、コンテナ名（`http://billiards-backend:8000`）を指定するだけで安全かつ確実に通信が行われます。

   **ディレクトリ構成**
   .
├── docker-compose.yml          # コンテナ全体を管理・起動する設定ファイル
├── backend/                    # バックエンド（Python / FastAPI）の部屋
│   ├── Dockerfile              # バックエンド専用のコンテナ設計図
│   ├── main.py                 # FastAPIのメインプログラム（メール送信処理等）
│   └── requirements.txt        # 必要なPythonライブラリの一覧（fastapi, uvicorn等）
└── frontend/                   # フロントエンド（Nginx / 静的ファイル）の部屋
    ├── Dockerfile              # フロントエンド専用のコンテナ設計図
    ├── index.html              # サイトの見た目（HTML / script.js?v=2 を読み込み）
    ├── script.js               # サイトの動き（お問い合せ送信処理、API_BASE=""）
    └── style.css               # サイトのデザイン（CSS）
